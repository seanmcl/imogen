-*- mode:org -*-

* Std

Rename ord-{set,map} to {set,map}

* Prop

remove kept and active rule sequents that are subsumed by ASD

only attempt to match rules that show as matches via the index
 currently all rules are attempted

max kept seqs is always 0

filter the conflicts by the preds occurring in initial rules and sequents

* FOL

Fix substitution tree indexing

* ALL

remove unused bindings.  search for val _ = ...
go through CRs

justify transparent ascriptions

understand mlton warnUnused annotations for std
